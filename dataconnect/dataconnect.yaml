specVersion: "v1"
serviceId: "bookstore-locator"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bookstore-locator-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
